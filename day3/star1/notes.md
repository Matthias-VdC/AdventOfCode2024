mul(x,y) 1-3 digit numbers

# The following should not work
mul(4*
mul(6,9!
?(12,34)
mul ( 2 , 4 )

# only 4 things work in this string
x<b>mul(2,4)</b>%&mul[3,7]!@^do_not_<b>mul(5,5)</b>+mul(32,64]then<b>(mul(11,8)mul(8,5)</b>)